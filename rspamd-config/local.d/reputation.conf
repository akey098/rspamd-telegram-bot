# Enable reputation plugin
enabled = true;

# Configure Redis backend
backend = "redis";
servers = "127.0.0.1:6379";

# Note: Telegram user reputation tracking is handled by Lua scripts
# in lua.local.d/telegram_simple.lua instead of the built-in reputation module
# since we need to extract custom headers (X-Telegram-User) 