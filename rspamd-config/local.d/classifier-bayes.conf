# Local configuration override for Bayesian classifier
# This allows learning via API calls (when no queue ID is present)

classifier "bayes" {
  # Override the learn condition to allow API calls
  learn_condition = 'return task:get_queue_id() or require("lua_bayes_learn").can_learn(task, is_spam, is_unlearn)';
}
