# Telegram Bot Custom Actions
# Define custom actions for telegram spam detection

# Note: Custom actions are not essential for basic functionality
# The default actions (reject, add_header, greylist) are sufficient 