# Lua configuration for Telegram bot rules
# This file loads the Telegram Lua modules

lua {
    # Load the telegram modular rules
    path = "/etc/rspamd/lua.local.d/";
    modules = ["telegram_modular"];
} 